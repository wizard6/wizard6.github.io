# progressive-build

Cursor / Grok Bot skill: **demo-first** delivery in fixed order.

1. **简单架构设计** — including mandatory **落地形式** gaps (host / runtime / entry / …)
2. **可运行竖切骨架 + 增量交付** — Walking Skeleton + Incremental, hard confirm gates
3. **演化式迭代** — Evolutionary Iterative product growth (Spiral optional, high-risk only)

Copy `SKILL.md` into an agent’s skills/workflows folder, or invoke via the bot that already has it.

Source of truth for this published copy: this path on `wizard6.github.io` (`master`).
